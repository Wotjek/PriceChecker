requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0
openpyxl>=3.1
Brotli>=1.1          # requests deklaruje wtedy Accept-Encoding: br, jak przegladarka
# Pin: na 1.62.0 po bledzie przegladarki polaczenie z Chromium sie klinuje
# i kolejne wywolanie sync API wisi bez timeoutu (runy 01-02.08.2026 ubite
# po 6h). Przed podbiciem wersji sprawdz pelny run.
playwright==1.61.0   # headless Chromium - fallback na sklepy z blokada botow
